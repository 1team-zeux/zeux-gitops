apiVersion: v1
kind: Service
metadata:
  name: atlantis-svc
  namespace: {{ .Values.global.namespace }}
  labels:
    app: atlantis
spec:
  selector:
    app: atlantis
  ports:
    - port: 4141
      targetPort: 4141
