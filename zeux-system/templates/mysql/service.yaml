apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: 1team-mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None
