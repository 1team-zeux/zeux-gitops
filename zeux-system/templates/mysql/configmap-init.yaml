apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-sql
  namespace: {{ .Values.global.namespace }}
data:
  v1_init_schema.sql: |
    -- MySQL Script generated by MySQL Workbench
    -- Tue Jun  9 16:40:42 2026
    -- Model: New Model    Version: 1.0
    -- MySQL Workbench Forward Engineering

    SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
    SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
    SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

    CREATE SCHEMA IF NOT EXISTS `zeux` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci ;
    USE `zeux` ;

    CREATE TABLE IF NOT EXISTS `zeux`.`customers` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `customer_name` VARCHAR(255) NOT NULL,
      `customer_code` VARCHAR(100) NOT NULL,
      `contact_email` VARCHAR(255) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE UNIQUE INDEX `customer_code` ON `zeux`.`customers` (`customer_code` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`business_units` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `customer_id` BIGINT NOT NULL,
      `bu_name` VARCHAR(255) NOT NULL,
      `bu_code` VARCHAR(100) NULL DEFAULT NULL,
      `application_name` VARCHAR(255) NULL DEFAULT NULL,
      `manager_email` VARCHAR(255) NULL DEFAULT NULL,
      `business_domain` VARCHAR(50) NULL DEFAULT NULL,
      `business_description` VARCHAR(255) NULL DEFAULT NULL,
      `contract_start_date` DATE NULL DEFAULT NULL,
      `contract_end_date` DATE NULL DEFAULT NULL,
      `subscription_tier` VARCHAR(50) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `fk_bu_customer`
        FOREIGN KEY (`customer_id`)
        REFERENCES `zeux`.`customers` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `fk_bu_customer` ON `zeux`.`business_units` (`customer_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`service_catalogs` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `business_unit_id` BIGINT NOT NULL,
      `service_name` VARCHAR(255) NOT NULL,
      `service_type` VARCHAR(50) NULL DEFAULT NULL,
      `description` TEXT NULL DEFAULT NULL,
      `service_tier` VARCHAR(20) NULL DEFAULT NULL,
      `criticality_score` INT NULL DEFAULT NULL,
      `user_target` VARCHAR(50) NULL DEFAULT NULL,
      `workload_type` VARCHAR(30) NULL DEFAULT NULL,
      `repository_url` VARCHAR(500) NULL DEFAULT NULL,
      `environment` VARCHAR(20) NULL DEFAULT NULL,
      `external_api_call` TINYINT(1) NULL DEFAULT NULL,
      `external_api_description` VARCHAR(255) NULL DEFAULT NULL,
      `container_deployable` TINYINT(1) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `fk_service_bu`
        FOREIGN KEY (`business_unit_id`)
        REFERENCES `zeux`.`business_units` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `fk_service_bu` ON `zeux`.`service_catalogs` (`business_unit_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`incidents` (
      `id` CHAR(36) NOT NULL,
      `service_catalog_id` BIGINT NOT NULL,
      `severity` VARCHAR(20) NULL DEFAULT NULL,
      `status` VARCHAR(30) NULL DEFAULT NULL,
      `title` VARCHAR(255) NULL DEFAULT NULL,
      `description` TEXT NULL DEFAULT NULL,
      `anomaly_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `confidence_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `started_at` TIMESTAMP NULL DEFAULT NULL,
      `resolved_at` TIMESTAMP NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `incidents_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`incidents` (`service_catalog_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`rca_results` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `incident_id` CHAR(36) NULL DEFAULT NULL,
      `root_cause_type` VARCHAR(50) NULL DEFAULT NULL,
      `root_cause_summary` TEXT NULL DEFAULT NULL,
      `impact_sli` VARCHAR(50) NULL DEFAULT NULL,
      `confidence_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `recommendation` TEXT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `rca_results_ibfk_1`
        FOREIGN KEY (`incident_id`)
        REFERENCES `zeux`.`incidents` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE UNIQUE INDEX `incident_id` ON `zeux`.`rca_results` (`incident_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`action_histories` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `incident_id` CHAR(36) NULL DEFAULT NULL,
      `rca_result_id` BIGINT NULL DEFAULT NULL,
      `action_type` VARCHAR(50) NULL DEFAULT NULL,
      `actor_type` VARCHAR(30) NULL DEFAULT NULL,
      `actor_name` VARCHAR(100) NULL DEFAULT NULL,
      `action_summary` TEXT NULL DEFAULT NULL,
      `execution_result` VARCHAR(50) NULL DEFAULT NULL,
      `execution_log` TEXT NULL DEFAULT NULL,
      `started_at` TIMESTAMP NULL DEFAULT NULL,
      `completed_at` TIMESTAMP NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `action_histories_ibfk_1`
        FOREIGN KEY (`incident_id`)
        REFERENCES `zeux`.`incidents` (`id`),
      CONSTRAINT `action_histories_ibfk_2`
        FOREIGN KEY (`rca_result_id`)
        REFERENCES `zeux`.`rca_results` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `incident_id` ON `zeux`.`action_histories` (`incident_id` ASC) VISIBLE;
    CREATE INDEX `rca_result_id` ON `zeux`.`action_histories` (`rca_result_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`alert_rules` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `service_catalog_id` BIGINT NOT NULL,
      `rule_name` VARCHAR(255) NULL DEFAULT NULL,
      `alert_category` VARCHAR(30) NULL DEFAULT NULL,
      `metric_name` VARCHAR(100) NULL DEFAULT NULL,
      `operator` VARCHAR(20) NULL DEFAULT NULL,
      `threshold` DECIMAL(15,4) NULL DEFAULT NULL,
      `evaluation_window` VARCHAR(50) NULL DEFAULT NULL,
      `notification_channel` VARCHAR(100) NULL DEFAULT NULL,
      `severity` VARCHAR(20) NULL DEFAULT NULL,
      `source` VARCHAR(20) NULL DEFAULT NULL,
      `is_active` TINYINT(1) NULL DEFAULT '1',
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `alert_rules_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`alert_rules` (`service_catalog_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`alarm_events` (
      `id` CHAR(36) NOT NULL,
      `service_catalog_id` BIGINT NOT NULL,
      `alert_rule_id` BIGINT NULL DEFAULT NULL,
      `source` VARCHAR(30) NULL DEFAULT NULL,
      `severity` VARCHAR(20) NULL DEFAULT NULL,
      `alarm_message` TEXT NULL DEFAULT NULL,
      `occurred_at` TIMESTAMP NULL DEFAULT NULL,
      `correlation_group_id` VARCHAR(100) NULL DEFAULT NULL,
      `status` VARCHAR(20) NULL DEFAULT NULL,
      `handoff_to_aiops` TINYINT(1) NULL DEFAULT '0',
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `alarm_events_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`),
      CONSTRAINT `alarm_events_ibfk_3`
        FOREIGN KEY (`alert_rule_id`)
        REFERENCES `zeux`.`alert_rules` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`alarm_events` (`service_catalog_id` ASC) VISIBLE;
    CREATE INDEX `alert_rule_id` ON `zeux`.`alarm_events` (`alert_rule_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`anomaly_results` (
      `id` CHAR(36) NOT NULL,
      `incident_id` CHAR(36) NULL DEFAULT NULL,
      `anomaly_type` VARCHAR(50) NULL DEFAULT NULL,
      `metric_name` VARCHAR(100) NULL DEFAULT NULL,
      `baseline_value` DECIMAL(20,4) NULL DEFAULT NULL,
      `observed_value` DECIMAL(20,4) NULL DEFAULT NULL,
      `anomaly_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `confidence_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `severity` VARCHAR(20) NULL DEFAULT NULL,
      `detected_at` TIMESTAMP NULL DEFAULT NULL,
      `status` VARCHAR(20) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY (`id`),
      CONSTRAINT `anomaly_results_ibfk_2`
        FOREIGN KEY (`incident_id`)
        REFERENCES `zeux`.`incidents` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `incident_id` ON `zeux`.`anomaly_results` (`incident_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`anomaly_evidences` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `anomaly_result_id` CHAR(36) NOT NULL,
      `evidence_type` VARCHAR(50) NULL DEFAULT NULL,
      `evidence_key` VARCHAR(255) NULL DEFAULT NULL,
      `evidence_value` TEXT NULL DEFAULT NULL,
      `score` DECIMAL(10,2) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY (`id`),
      CONSTRAINT `anomaly_evidences_ibfk_1`
        FOREIGN KEY (`anomaly_result_id`)
        REFERENCES `zeux`.`anomaly_results` (`id`)
        ON DELETE CASCADE)
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `anomaly_result_id` ON `zeux`.`anomaly_evidences` (`anomaly_result_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`sla_policies` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `service_catalog_id` BIGINT NOT NULL,
      `category` VARCHAR(50) NULL DEFAULT NULL,
      `sla_level` VARCHAR(20) NULL DEFAULT 'SERVICE',
      `operator` VARCHAR(20) NULL DEFAULT NULL,
      `target_value` DECIMAL(10,2) NULL DEFAULT NULL,
      `unit` VARCHAR(20) NULL DEFAULT NULL,
      `measurement_window` VARCHAR(50) NULL DEFAULT NULL,
      `measurement_formula` TEXT NULL DEFAULT NULL,
      `measurement_point` VARCHAR(100) NULL DEFAULT NULL,
      `measurement_filter` JSON NULL DEFAULT NULL,
      `exclusion_conditions` TEXT NULL DEFAULT NULL,
      `service_credit_ladder` JSON NULL DEFAULT NULL,
      `is_active` TINYINT(1) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `sla_policies_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`sla_policies` (`service_catalog_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`burn_rate_status` (
      `id` CHAR(36) NOT NULL,
      `sla_policy_id` BIGINT NOT NULL,
      `judgment_mode` VARCHAR(20) NULL DEFAULT NULL,
      `burn_rate_value` DECIMAL(10,2) NULL DEFAULT NULL,
      `burn_rate_state` VARCHAR(20) NULL DEFAULT NULL,
      `short_window` VARCHAR(30) NULL DEFAULT NULL,
      `long_window` VARCHAR(30) NULL DEFAULT NULL,
      `evaluated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `burn_rate_status_ibfk_2`
        FOREIGN KEY (`sla_policy_id`)
        REFERENCES `zeux`.`sla_policies` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `sla_policy_id` ON `zeux`.`burn_rate_status` (`sla_policy_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`business_unit_requirements` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `business_unit_id` BIGINT NOT NULL,
      `csp` VARCHAR(30) NULL DEFAULT NULL,
      `primary_region` VARCHAR(50) NULL DEFAULT NULL,
      `multi_az_required` TINYINT(1) NULL DEFAULT '0',
      `multi_region_required` TINYINT(1) NULL DEFAULT '0',
      `failover_required` TINYINT(1) NULL DEFAULT '0',
      `new_vpc_required` TINYINT(1) NULL DEFAULT '1',
      `existing_network_integration` TINYINT(1) NULL DEFAULT '0',
      `vpn_required` TINYINT(1) NULL DEFAULT '0',
      `internal_service_external_exposure` TINYINT(1) NULL DEFAULT '0',
      `internet_ingress_required` TINYINT(1) NULL DEFAULT '0',
      `db_required` TINYINT(1) NULL DEFAULT '0',
      `data_type` VARCHAR(50) NULL DEFAULT NULL,
      `consistency_priority` VARCHAR(20) NULL DEFAULT NULL,
      `read_write_ratio` VARCHAR(20) NULL DEFAULT NULL,
      `initial_data_volume` VARCHAR(50) NULL DEFAULT NULL,
      `data_growth_rate` VARCHAR(50) NULL DEFAULT NULL,
      `backup_required` TINYINT(1) NULL DEFAULT '0',
      `db_external_access` TINYINT(1) NULL DEFAULT '0',
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `business_unit_requirements_ibfk_1`
        FOREIGN KEY (`business_unit_id`)
        REFERENCES `zeux`.`business_units` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `business_unit_id` ON `zeux`.`business_unit_requirements` (`business_unit_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`cost_constraints` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `business_unit_id` BIGINT NOT NULL,
      `monthly_budget` DECIMAL(15,2) NULL DEFAULT NULL,
      `cost_priority` VARCHAR(30) NULL DEFAULT NULL,
      `managed_service_scope` VARCHAR(30) NULL DEFAULT NULL,
      `spot_instance_allowed` TINYINT(1) NULL DEFAULT '0',
      `reserved_instance_preferred` TINYINT(1) NULL DEFAULT '0',
      `auto_scaling_required` TINYINT(1) NULL DEFAULT '1',
      `cost_alert_threshold` INT NULL DEFAULT NULL,
      `max_compute_instance_count` INT NULL DEFAULT NULL,
      `max_database_instance_count` INT NULL DEFAULT NULL,
      `max_storage_size_gb` INT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `cost_constraints_ibfk_1`
        FOREIGN KEY (`business_unit_id`)
        REFERENCES `zeux`.`business_units` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `business_unit_id` ON `zeux`.`cost_constraints` (`business_unit_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`topologies` (
      `id` CHAR(36) NOT NULL,
      `business_unit_id` BIGINT NOT NULL,
      `bundle_version` VARCHAR(50) NULL DEFAULT NULL,
      `concept` VARCHAR(20) NULL DEFAULT NULL,
      `status` VARCHAR(20) NULL DEFAULT NULL,
      `base_id` VARCHAR(50) NULL DEFAULT NULL,
      `catalog_version` VARCHAR(50) NULL DEFAULT NULL,
      `expected_monthly_cost_min` DECIMAL(15,2) NULL DEFAULT NULL,
      `expected_monthly_cost_max` DECIMAL(15,2) NULL DEFAULT NULL,
      `generated_at` TIMESTAMP NULL DEFAULT NULL,
      `approved_at` TIMESTAMP NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `topologies_ibfk_1`
        FOREIGN KEY (`business_unit_id`)
        REFERENCES `zeux`.`business_units` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `business_unit_id` ON `zeux`.`topologies` (`business_unit_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`topology_decisions` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `topology_id` CHAR(36) NOT NULL,
      `dimension` VARCHAR(30) NULL DEFAULT NULL,
      `decision_code` VARCHAR(20) NULL DEFAULT NULL,
      `decision_label` VARCHAR(255) NULL DEFAULT NULL,
      `rule_applied` VARCHAR(100) NULL DEFAULT NULL,
      `rationale_md` TEXT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `topology_decisions_ibfk_1`
        FOREIGN KEY (`topology_id`)
        REFERENCES `zeux`.`topologies` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `topology_id` ON `zeux`.`topology_decisions` (`topology_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`decision_inputs` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `topology_decision_id` BIGINT NOT NULL,
      `input_type` VARCHAR(30) NULL DEFAULT NULL,
      `input_id` BIGINT NULL DEFAULT NULL,
      `contribution` TEXT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `decision_inputs_ibfk_1`
        FOREIGN KEY (`topology_decision_id`)
        REFERENCES `zeux`.`topology_decisions` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `topology_decision_id` ON `zeux`.`decision_inputs` (`topology_decision_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`error_budgets` (
      `id` CHAR(36) NOT NULL,
      `service_catalog_id` BIGINT NOT NULL,
      `sla_policy_id` BIGINT NOT NULL,
      `period_start` TIMESTAMP NULL DEFAULT NULL,
      `period_end` TIMESTAMP NULL DEFAULT NULL,
      `budget_total_value` DECIMAL(15,4) NULL DEFAULT NULL,
      `budget_total_unit` VARCHAR(20) NULL DEFAULT NULL,
      `budget_consumed_value` DECIMAL(15,4) NULL DEFAULT NULL,
      `budget_consumed_unit` VARCHAR(20) NULL DEFAULT NULL,
      `budget_remaining_percent` DECIMAL(6,3) NULL DEFAULT NULL,
      `last_calculated_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `error_budgets_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`),
      CONSTRAINT `error_budgets_ibfk_2`
        FOREIGN KEY (`sla_policy_id`)
        REFERENCES `zeux`.`sla_policies` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`error_budgets` (`service_catalog_id` ASC) VISIBLE;
    CREATE INDEX `sla_policy_id` ON `zeux`.`error_budgets` (`sla_policy_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`resources` (
      `id` CHAR(36) NOT NULL,
      `service_catalog_id` BIGINT NOT NULL,
      `provider` VARCHAR(30) NULL DEFAULT NULL,
      `resource_type` VARCHAR(100) NULL DEFAULT NULL,
      `resource_name` VARCHAR(255) NULL DEFAULT NULL,
      `physical_id` VARCHAR(500) NULL DEFAULT NULL,
      `region` VARCHAR(100) NULL DEFAULT NULL,
      `status` VARCHAR(50) NULL DEFAULT NULL,
      `tags` JSON NULL DEFAULT NULL,
      `topology_decision_id` BIGINT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `resources_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`),
      CONSTRAINT `resources_ibfk_2`
        FOREIGN KEY (`topology_decision_id`)
        REFERENCES `zeux`.`topology_decisions` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE UNIQUE INDEX `physical_id` ON `zeux`.`resources` (`physical_id` ASC) VISIBLE;
    CREATE INDEX `service_catalog_id` ON `zeux`.`resources` (`service_catalog_id` ASC) VISIBLE;
    CREATE INDEX `topology_decision_id` ON `zeux`.`resources` (`topology_decision_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`rca_affected_resources` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `rca_result_id` BIGINT NOT NULL,
      `resource_id` CHAR(36) NOT NULL,
      `impact_type` VARCHAR(50) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `rca_affected_resources_ibfk_1`
        FOREIGN KEY (`rca_result_id`)
        REFERENCES `zeux`.`rca_results` (`id`),
      CONSTRAINT `rca_affected_resources_ibfk_2`
        FOREIGN KEY (`resource_id`)
        REFERENCES `zeux`.`resources` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `rca_result_id` ON `zeux`.`rca_affected_resources` (`rca_result_id` ASC) VISIBLE;
    CREATE INDEX `resource_id` ON `zeux`.`rca_affected_resources` (`resource_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`rca_evidences` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `rca_result_id` BIGINT NOT NULL,
      `evidence_type` VARCHAR(30) NULL DEFAULT NULL,
      `evidence_summary` TEXT NULL DEFAULT NULL,
      `evidence_score` DECIMAL(10,2) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `rca_evidences_ibfk_1`
        FOREIGN KEY (`rca_result_id`)
        REFERENCES `zeux`.`rca_results` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `rca_result_id` ON `zeux`.`rca_evidences` (`rca_result_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`resource_dependencies` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `source_resource_id` CHAR(36) NULL DEFAULT NULL,
      `target_resource_id` CHAR(36) NULL DEFAULT NULL,
      `dependency_type` VARCHAR(50) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `resource_dependencies_ibfk_1`
        FOREIGN KEY (`source_resource_id`)
        REFERENCES `zeux`.`resources` (`id`),
      CONSTRAINT `resource_dependencies_ibfk_2`
        FOREIGN KEY (`target_resource_id`)
        REFERENCES `zeux`.`resources` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `source_resource_id` ON `zeux`.`resource_dependencies` (`source_resource_id` ASC) VISIBLE;
    CREATE INDEX `target_resource_id` ON `zeux`.`resource_dependencies` (`target_resource_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`service_dependencies` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `source_service_id` BIGINT NOT NULL,
      `target_service_id` BIGINT NOT NULL,
      `dependency_type` VARCHAR(50) NULL DEFAULT NULL,
      `criticality_weight` DECIMAL(5,2) NULL DEFAULT '1.00',
      `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY (`id`),
      CONSTRAINT `service_dependencies_ibfk_1`
        FOREIGN KEY (`source_service_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`),
      CONSTRAINT `service_dependencies_ibfk_2`
        FOREIGN KEY (`target_service_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `source_service_id` ON `zeux`.`service_dependencies` (`source_service_id` ASC) VISIBLE;
    CREATE INDEX `target_service_id` ON `zeux`.`service_dependencies` (`target_service_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`sli_records` (
      `id` CHAR(36) NOT NULL,
      `service_catalog_id` BIGINT NOT NULL,
      `sla_policy_id` BIGINT NOT NULL,
      `category` VARCHAR(50) NULL DEFAULT NULL,
      `sla_level` VARCHAR(20) NULL DEFAULT NULL,
      `measured_value` DECIMAL(15,4) NULL DEFAULT NULL,
      `measured_unit` VARCHAR(20) NULL DEFAULT NULL,
      `measurement_point` VARCHAR(100) NULL DEFAULT NULL,
      `window_start` TIMESTAMP NULL DEFAULT NULL,
      `window_end` TIMESTAMP NULL DEFAULT NULL,
      `sample_count` BIGINT NULL DEFAULT NULL,
      `is_excluded` TINYINT(1) NULL DEFAULT '0',
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `sli_records_ibfk_1`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`),
      CONSTRAINT `sli_records_ibfk_2`
        FOREIGN KEY (`sla_policy_id`)
        REFERENCES `zeux`.`sla_policies` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `service_catalog_id` ON `zeux`.`sli_records` (`service_catalog_id` ASC) VISIBLE;
    CREATE INDEX `sla_policy_id` ON `zeux`.`sli_records` (`sla_policy_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`terraform_artifacts` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `topology_id` CHAR(36) NOT NULL,
      `hcl_files` JSON NULL DEFAULT NULL,
      `validate_result` VARCHAR(20) NULL DEFAULT NULL,
      `fmt_result` VARCHAR(20) NULL DEFAULT NULL,
      `tflint_result` VARCHAR(20) NULL DEFAULT NULL,
      `tfsec_result` VARCHAR(20) NULL DEFAULT NULL,
      `validation_detail` JSON NULL DEFAULT NULL,
      `plan_summary` JSON NULL DEFAULT NULL,
      `workspace_status` VARCHAR(30) NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      `updated_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `terraform_artifacts_ibfk_1`
        FOREIGN KEY (`topology_id`)
        REFERENCES `zeux`.`topologies` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `topology_id` ON `zeux`.`terraform_artifacts` (`topology_id` ASC) VISIBLE;

    CREATE TABLE IF NOT EXISTS `zeux`.`traffic_profiles` (
      `id` BIGINT NOT NULL AUTO_INCREMENT,
      `service_catalog_id` BIGINT NOT NULL,
      `expected_avg_rps` INT NULL DEFAULT NULL,
      `expected_peak_rps` INT NULL DEFAULT NULL,
      `expected_max_concurrent_users` INT NULL DEFAULT NULL,
      `expected_peak_write_rate` INT NULL DEFAULT NULL,
      `traffic_pattern` VARCHAR(50) NULL DEFAULT NULL,
      `user_geo_distribution` TEXT NULL DEFAULT NULL,
      `created_at` TIMESTAMP NULL DEFAULT NULL,
      PRIMARY KEY (`id`),
      CONSTRAINT `fk_tp_service`
        FOREIGN KEY (`service_catalog_id`)
        REFERENCES `zeux`.`service_catalogs` (`id`))
    ENGINE = InnoDB
    DEFAULT CHARACTER SET = utf8mb4
    COLLATE = utf8mb4_0900_ai_ci;

    CREATE INDEX `fk_tp_service` ON `zeux`.`traffic_profiles` (`service_catalog_id` ASC) VISIBLE;

    SET SQL_MODE=@OLD_SQL_MODE;
    SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
    SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
